// Obsolete: Order price component under _conceptions was removed in favor of shared Price component.
// Keeping this file to avoid module not found errors in CI; skipping the suite.
describe.skip('components/Orders/Price (obsolete)', () => {
	it('skipped', () => {
		expect(true).toBe(true);
	});
});
